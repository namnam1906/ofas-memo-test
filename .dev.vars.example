# Copy to .dev.vars for local `npm run dev` (that file is gitignored).
# In production these are set with `wrangler secret put <NAME>` instead —
# never put real keys in wrangler.toml or commit them.

ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=...
